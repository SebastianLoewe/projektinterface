var x = 0;

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  strokeWeight(10);
  for (var x = 0; x <= width; x += 40) {
    //fill (x, 150, 100, 150)
    //stroke(x);
    ellipse(x, 200, 25, 25);
  }
}
