let an = true;

function setup() {
  createCanvas(400, 400);
}

function draw() {}

function mousePressed() {  
  if (an) {
    background(0, 220, 0);
    an = false; 
  } else {
    background(0);
    an = true;
  }
}
