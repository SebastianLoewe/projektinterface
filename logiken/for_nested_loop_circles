function setup() {
  createCanvas(800, 800);
}

function draw() {
  background(250);
  strokeWeight(10);
  for (let x = 0; x <= width; x = x + 40) {
    for (let y = 0; y <= width; y += 40) {
      circle(x, y, 25);
    }
  }
}
