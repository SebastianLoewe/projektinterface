let r = 0;
let g = 0;
let b = 0;
let y = 0;

function setup() {
  createCanvas(400, 400);
  background(255);
}

function draw() {
  strokeWeight(5);
  stroke(r, g, b);
  line(0, y, width, y); 
  y = y + 10; //y += 10; 
  r = r + 10; //r += 10;
}
