function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  strokeWeight(10);
  for (let x = 0; x <= width; x += 40) {
    //fill (x, 150, 100, 150)
    //stroke(x);
    circle(x, 200, 25);
  }
}
