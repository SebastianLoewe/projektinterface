var r = 0;
var g = 0;
var b = 0;
var y = 0;

function setup() {
  createCanvas(400, 400);
  background(255);
}

function draw() {
  strokeWeight(5);
  stroke(r, g, b);
  line(0, y, width, y); 
  y = y + 10; //y += 10; 
  r = r + 10; //r += 10;
}
