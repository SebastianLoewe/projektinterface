function setup() {
  createCanvas(800, 800);
}

function draw() {
  background(250);
  strokeWeight(10);
  for (var x = 0; x <= width; x = x + 40) {
    for (var y = 0; y <= width; y += 40) {
      ellipse(x, y, 25, 25);
    }
  }
}
